import { BuscarNumeroDirective } from './buscar-numero.directive';

describe('BuscarNumeroDirective', () => {
  it('should create an instance', () => {
    const directive = new BuscarNumeroDirective();
    expect(directive).toBeTruthy();
  });
});

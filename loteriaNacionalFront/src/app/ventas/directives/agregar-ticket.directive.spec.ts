import { AgregarTicketDirective } from './agregar-ticket.directive';

describe('AgregarTicketDirective', () => {
  it('should create an instance', () => {
    const directive = new AgregarTicketDirective();
    expect(directive).toBeTruthy();
  });
});

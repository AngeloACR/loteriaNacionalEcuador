import { BorrarTicketDirective } from './borrar-ticket.directive';

describe('BorrarTicketDirective', () => {
  it('should create an instance', () => {
    const directive = new BorrarTicketDirective();
    expect(directive).toBeTruthy();
  });
});

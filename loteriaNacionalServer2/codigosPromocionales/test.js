const fs = require("fs").promises;
const config = require("../environments/test");
const CodigoPromocional = require("./models/main")(async () => {
  try {
  } catch (error) {
    console.log(error.message);
  }
})();
